You are a resume parsing engine. Read the resume text below and extract
structured information.

Return ONLY valid JSON. No explanation, no markdown code fences, no text
before or after the JSON. Just the JSON object itself.

Use exactly this structure:

{
  "skills": ["list", "of", "technical", "and", "soft", "skills"],
  "yearsExperience": <number, estimate from work history if not stated>,
  "education": [
    {"degree": "", "institution": "", "year": ""}
  ],
  "projects": [
    {"name": "", "description": ""}
  ],
  "domain": "e.g. fintech, healthcare, e-commerce, general software",
  "currentRole": "most recent job title, or null if fresher"
}

Rules:
- If a field cannot be found, use null (for single values) or an empty
  array (for lists). Never omit a key.
- Include skills mentioned anywhere in the resume, not just a dedicated
  "Skills" section - also check project descriptions and work experience.
- For yearsExperience, calculate from the earliest job start date to the
  most recent job end date (or "present") if a total isn't stated
  explicitly. Exclude any explicitly stated employment gaps from the count.
- Keep skill names concise (e.g. "Python", not "Proficient in Python").

Resume text:
---
{resume_text}
---
